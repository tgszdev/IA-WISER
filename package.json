{
  "name": "wiser-ia-assistant",
  "version": "1.0.0",
  "description": "Assistente de IA configurável com base de conhecimento personalizada",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build needed for Vercel'",
    "deploy": "vercel --prod",
    "test": "node --version"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@vercel/edge-config": "^1.3.0",
    "postgres": "^3.4.7",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}